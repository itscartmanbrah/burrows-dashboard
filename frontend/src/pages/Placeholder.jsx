// Generic placeholder page used for tools that haven't been built yet.

export default function Placeholder({ title }) {
  return (
    <div>
      <h2>{title}</h2>
      <p>This tool hasn't been built yet — coming in a later phase.</p>
    </div>
  );
}
